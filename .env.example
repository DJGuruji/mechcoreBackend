# WebSocket Relay Server Environment Configuration

# Server Port
PORT=8080

# Node Environment (development, production)
NODE_ENV=production

# Allowed Origins (comma-separated list of frontend URLs)
# Add your production frontend URL here
ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend-domain.com,https://your-production-url.vercel.app

# Session Configuration
SESSION_TIMEOUT_MINUTES=10
REQUEST_TIMEOUT_SECONDS=30

# Cleanup Interval (minutes)
CLEANUP_INTERVAL_MINUTES=5
